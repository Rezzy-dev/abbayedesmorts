You'll need to use the old toolchain to compile this game.

Under the "bittboy" platform in the Makefile, point CC, CFLAGS, SDL_CFLAGS, and SDL_LIBS to the path where the old Miyoo toolchain is installed.

Compile with "make bittboy".
